package synchronization;

public class ATM {
	static void withdraw(BankAccount account, int account) {
		int balance = account.getbalance();
		if ((balance - amount) < - account.getOverdraft()) {
			System.out.println("Transaction denied");
		} else {
			account.debit(;)
		}
	}
}
